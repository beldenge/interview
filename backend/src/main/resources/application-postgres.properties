spring.h2.console.enabled=false
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost:5432/interview}
spring.datasource.username=${POSTGRES_USER:interview}
spring.datasource.password=${POSTGRES_PASSWORD:interview}
